% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gdalraster.R, R/speckle.R, R/terra.R
\name{rf_lee_sigma_improved.Rcpp_GDALRaster}
\alias{rf_lee_sigma_improved.Rcpp_GDALRaster}
\alias{rf_lee_sigma_improved}
\alias{rf_lee_sigma_improved.matrix}
\alias{rf_lee_sigma_improved.array}
\alias{rf_lee_sigma_improved.default}
\alias{rf_lee_sigma_improved.SpatRaster}
\title{Improved Lee sigma speckle filter}
\usage{
\method{rf_lee_sigma_improved}{Rcpp_GDALRaster}(
  x,
  window = 7L,
  looks = 1,
  sigma = 0.9,
  target_window = 3L,
  edge = c("shrink", "reflect", "nearest", "constant"),
  edge_value = 0,
  na_policy = c("omit", "propagate"),
  ...
)

rf_lee_sigma_improved(x, ...)

\method{rf_lee_sigma_improved}{matrix}(
  x,
  window = 7L,
  looks = 1,
  sigma = 0.9,
  target_window = 3L,
  edge = c("shrink", "reflect", "nearest", "constant"),
  edge_value = 0,
  na_policy = c("omit", "propagate"),
  ...
)

\method{rf_lee_sigma_improved}{array}(
  x,
  window = 7L,
  looks = 1,
  sigma = 0.9,
  target_window = 3L,
  edge = c("shrink", "reflect", "nearest", "constant"),
  edge_value = 0,
  na_policy = c("omit", "propagate"),
  ...
)

\method{rf_lee_sigma_improved}{default}(x, ...)

\method{rf_lee_sigma_improved}{SpatRaster}(x, ...)
}
\arguments{
\item{x}{A numeric matrix or 3-D array (filtered layer by layer). Methods
are also provided for terra \code{SpatRaster} objects (when terra is
installed) and for open gdalraster \code{GDALRaster} datasets (when
gdalraster is installed). \code{GDALRaster} methods return a new
\code{GDALRaster} object open in update mode on a Float64 dataset with the
source's geometry. Small datasets are filtered in memory and land on an
in-memory \verb{/vsimem} GTiff by default; datasets whose decoded size
exceeds \code{options(rustyfilters.block_memory)} (default 2 GiB) stream
through full-width row bands with a halo sized to the filter's window,
writing to a GeoTIFF tempfile instead. Interior band seams are exact
(the halo supplies the true neighbouring data; \code{edge} fires only at
real raster edges). \code{GDALRaster} methods accept three extra arguments:
\code{filename} (output path, replacing the tempfile/\verb{/vsimem} default),
\code{by_block} (\code{TRUE}/\code{FALSE} to force or forbid streaming) and
\code{block_rows} (rows per band, sized from the memory budget by default).}

\item{window}{Window size in cells: a single odd positive integer, or a
pair \code{c(rows, cols)} of odd positive integers.}

\item{looks}{Effective number of looks. Must be 1, 2, 3 or 4; the
published sigma range tables only cover these.}

\item{sigma}{Sigma confidence level: one of 0.5, 0.6, 0.7, 0.8 or 0.9
(the default, covering the widest speckle range).}

\item{target_window}{Single odd integer (typically 3 or 5): the window
used for the a priori mean estimate and for point-target detection.}

\item{edge}{How to treat windows that overhang the matrix edge:
\code{"shrink"} (default) truncates the window to the cells that exist;
\code{"reflect"} mirrors the matrix across the boundary; \code{"nearest"} repeats
the closest edge cell; \code{"constant"} pads with \code{edge_value}.}

\item{edge_value}{Single number used to pad when \code{edge = "constant"}.
\code{NA} is allowed and behaves as missing data under \code{na_policy = "omit"}.}

\item{na_policy}{How to treat missing values inside a window. The default
\code{"omit"} excludes them from the statistics (like \code{na.rm = TRUE}); a
window with no valid cells yields \code{NA}. \code{"propagate"} is the fast path:
no per-cell NA handling is compiled into the inner loop, and any \code{NA}
in a window makes the result \code{NA}. Use it when the input has no missing
values (or when spreading \code{NA} is acceptable) for maximum speed.}

\item{...}{Passed on to methods.}
}
\value{
An object of the same class and dimensions as \code{x} (dimnames are
preserved), containing the filtered values as doubles.
}
\description{
The improved sigma filter of Lee et al. (2009), as popularised by ESA
SNAP. It addresses the classic sigma filter's dark bias by (1) estimating
an a priori mean with a small MMSE filter and using published sigma range
bounds around it, (2) preserving point targets, detected as clusters of
pixels above the scene's 98th percentile, and (3) filtering the in-range
pixels with an MMSE weight based on a revised speckle variation
coefficient.
}
\details{
Point-target marking is order-independent here: a pixel is preserved when
it exceeds the layer's 98th percentile and lies within \code{target_window} of
a cluster of more than five such pixels. SNAP marks clusters during a
sequential scan, which can differ at cluster fringes. The 98th percentile
is computed per layer over valid cells.
}
\examples{
set.seed(1)
speckled <- matrix(rexp(400), 20, 20)
rf_lee_sigma_improved(speckled, window = 7L, looks = 1, sigma = 0.9)
}
\references{
Lee, J.-S., Wen, J.-H., Ainsworth, T. L., Chen, K.-S., &
Chen, A. J. (2009). Improved sigma filter for speckle filtering of SAR
imagery. \emph{IEEE Transactions on Geoscience and Remote Sensing}, 47(1),
202-213.
}
\seealso{
\code{\link[=rf_lee_sigma]{rf_lee_sigma()}} for the classic 1983 filter, \code{\link[=rf_lee]{rf_lee()}},
\code{\link[=rf_enhanced_lee]{rf_enhanced_lee()}}
}
