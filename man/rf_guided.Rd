% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/edge_preserving.R, R/terra.R
\name{rf_guided}
\alias{rf_guided}
\alias{rf_guided.matrix}
\alias{rf_guided.array}
\alias{rf_guided.default}
\alias{rf_guided.SpatRaster}
\title{Guided filter}
\usage{
rf_guided(x, ...)

\method{rf_guided}{matrix}(
  x,
  guide = NULL,
  window = 5L,
  eps = NULL,
  edge = c("shrink", "reflect", "nearest", "constant"),
  edge_value = 0,
  na_policy = c("omit", "propagate"),
  ...
)

\method{rf_guided}{array}(
  x,
  guide = NULL,
  window = 5L,
  eps = NULL,
  edge = c("shrink", "reflect", "nearest", "constant"),
  edge_value = 0,
  na_policy = c("omit", "propagate"),
  ...
)

\method{rf_guided}{default}(x, ...)

\method{rf_guided}{SpatRaster}(x, guide = NULL, ...)
}
\arguments{
\item{x}{A numeric matrix or 3-D array (filtered layer by layer). Methods
for terra \code{SpatRaster} objects are provided when terra is installed.}

\item{...}{Passed on to methods.}

\item{guide}{A matrix or 3-D array with the same dimensions as \code{x}, or
\code{NULL} (the default) to use \code{x} itself.}

\item{window}{Window size: a single odd integer or a \code{c(rows, cols)}
pair.}

\item{eps}{Single positive number: the regularisation added to the window
variance of the guide. Windows whose variance is well below \code{eps} are
smoothed; windows well above it (edges) are preserved. Scale-dependent:
the default \code{NULL} uses \code{(0.1 * sd(guide))^2}.}

\item{edge}{How to treat windows that overhang the matrix edge:
\code{"shrink"} (default) truncates the window to the cells that exist;
\code{"reflect"} mirrors the matrix across the boundary; \code{"nearest"} repeats
the closest edge cell; \code{"constant"} pads with \code{edge_value}.}

\item{edge_value}{Single number used to pad when \code{edge = "constant"}.
\code{NA} is allowed and behaves as missing data under \code{na_policy = "omit"}.}

\item{na_policy}{How to treat missing values inside a window. The default
\code{"omit"} excludes them from the statistics (like \code{na.rm = TRUE}); a
window with no valid cells yields \code{NA}. \code{"propagate"} is the fast path:
no per-cell NA handling is compiled into the inner loop, and any \code{NA}
in a window makes the result \code{NA}. Use it when the input has no missing
values (or when spreading \code{NA} is acceptable) for maximum speed.}
}
\value{
An object of the same class and dimensions as \code{x} (dimnames are
preserved), containing the filtered values as doubles.
}
\description{
The guided filter (He et al. 2013) fits a local linear model between a
guide image and the input in every window, then averages the coefficients:
an edge-preserving smoother whose cost is independent of the window size
(it runs entirely on the package's O(1) box-mean engine). With the default
self-guidance (\code{guide = NULL}) it behaves like a fast bilateral
alternative; with a separate \code{guide} it transfers the guide's structure
onto \code{x} (e.g. detail-preserving smoothing of a noisy band guided by a
clean one).
}
\details{
Missing cells in either \code{x} or the guide are excluded from every window
fit; cells whose centre is \code{NA} in the guide stay \code{NA} in the output.
}
\examples{
set.seed(1)
m <- matrix(rep(c(0, 10), each = 50), 10) + rnorm(100)
rf_guided(m, window = 5L)
rf_guided(m, window = 5L, eps = 100) # large eps: plain smoothing
}
\references{
He, K., Sun, J., & Tang, X. (2013). Guided image filtering.
\emph{IEEE Transactions on Pattern Analysis and Machine Intelligence},
35(6), 1397-1409.
}
\seealso{
\code{\link[=rf_bilateral]{rf_bilateral()}}, \code{\link[=rf_mean]{rf_mean()}}, \code{\link[=rf_set_threads]{rf_set_threads()}}
}
