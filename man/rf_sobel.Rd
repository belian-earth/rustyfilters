% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convolve.R, R/terra.R
\name{rf_sobel}
\alias{rf_sobel}
\alias{rf_sobel.matrix}
\alias{rf_sobel.array}
\alias{rf_sobel.default}
\alias{rf_sobel.SpatRaster}
\title{Sobel edge detection}
\usage{
rf_sobel(x, ...)

\method{rf_sobel}{matrix}(
  x,
  direction = c("magnitude", "x", "y"),
  edge = c("nearest", "shrink", "reflect", "constant"),
  edge_value = 0,
  na_policy = c("omit", "propagate"),
  ...
)

\method{rf_sobel}{array}(
  x,
  direction = c("magnitude", "x", "y"),
  edge = c("nearest", "shrink", "reflect", "constant"),
  edge_value = 0,
  na_policy = c("omit", "propagate"),
  ...
)

\method{rf_sobel}{default}(x, ...)

\method{rf_sobel}{SpatRaster}(x, ...)
}
\arguments{
\item{x}{A numeric matrix or 3-D array (filtered layer by layer). Methods
for terra \code{SpatRaster} objects are provided when terra is installed.}

\item{...}{Passed on to methods.}

\item{direction}{\code{"magnitude"}, \code{"x"} or \code{"y"}.}

\item{edge}{Edge policy, as in \code{\link[=rf_convolve]{rf_convolve()}}. Defaults to \code{"nearest"}
for gradients: replicating the border cell gives a near-zero gradient
there, whereas a shrinking window would fabricate strong edges.}

\item{edge_value}{Single number used to pad when \code{edge = "constant"}.
\code{NA} is allowed and behaves as missing data under \code{na_policy = "omit"}.}

\item{na_policy}{How to treat missing values inside a window. The default
\code{"omit"} excludes them from the statistics (like \code{na.rm = TRUE}); a
window with no valid cells yields \code{NA}. \code{"propagate"} is the fast path:
no per-cell NA handling is compiled into the inner loop, and any \code{NA}
in a window makes the result \code{NA}. Use it when the input has no missing
values (or when spreading \code{NA} is acceptable) for maximum speed.}
}
\value{
An object of the same class and dimensions as \code{x} (dimnames are
preserved), containing the filtered values as doubles.
}
\description{
Convolves with the 3 x 3 Sobel kernels to estimate the local gradient:
\code{"x"} is the rate of change across columns, \code{"y"} across rows, and
\code{"magnitude"} (the default) is \code{sqrt(gx^2 + gy^2)}.
}
\examples{
m <- matrix(rep(c(1, 1, 5, 5), each = 5), 5)
rf_sobel(m)
rf_sobel(m, direction = "x")
}
\seealso{
\code{\link[=rf_convolve]{rf_convolve()}}, \code{\link[=rf_laplacian]{rf_laplacian()}}
}
