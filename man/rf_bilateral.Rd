% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/edge_preserving.R, R/gdalraster.R, R/terra.R
\name{rf_bilateral}
\alias{rf_bilateral}
\alias{rf_bilateral.matrix}
\alias{rf_bilateral.array}
\alias{rf_bilateral.default}
\alias{rf_bilateral.Rcpp_GDALRaster}
\alias{rf_bilateral.SpatRaster}
\title{Bilateral filter}
\usage{
rf_bilateral(x, ...)

\method{rf_bilateral}{matrix}(
  x,
  sigma_d = 1.5,
  sigma_r = NULL,
  window = NULL,
  edge = c("shrink", "reflect", "nearest", "constant"),
  edge_value = 0,
  na_policy = c("omit", "propagate"),
  ...
)

\method{rf_bilateral}{array}(
  x,
  sigma_d = 1.5,
  sigma_r = NULL,
  window = NULL,
  edge = c("shrink", "reflect", "nearest", "constant"),
  edge_value = 0,
  na_policy = c("omit", "propagate"),
  ...
)

\method{rf_bilateral}{default}(x, ...)

\method{rf_bilateral}{Rcpp_GDALRaster}(x, ...)

\method{rf_bilateral}{SpatRaster}(x, ...)
}
\arguments{
\item{x}{A numeric matrix or 3-D array (filtered layer by layer). Methods
are also provided for terra \code{SpatRaster} objects (when terra is
installed) and for open gdalraster \code{GDALRaster} datasets (when
gdalraster is installed). \code{GDALRaster} methods read the dataset into
memory, filter it, and return a new \code{GDALRaster} object open in update
mode on a Float64 dataset with the source's geometry: an in-memory
\verb{/vsimem} GTiff by default, or pass \code{filename} to write to disk.}

\item{...}{Passed on to methods.}

\item{sigma_d}{Single positive number: the spatial standard deviation in
cells.}

\item{sigma_r}{Single positive number: the range standard deviation in
value units. Values differing from the centre by much more than
\code{sigma_r} are effectively excluded. The default \code{NULL} uses the standard
deviation of the valid cells of \code{x}, a serviceable starting point that
you should expect to tune.}

\item{window}{Window size: a single odd integer or a \code{c(rows, cols)}
pair. The default \code{NULL} uses \code{2 * ceiling(2 * sigma_d) + 1}.}

\item{edge}{How to treat windows that overhang the matrix edge:
\code{"shrink"} (default) truncates the window to the cells that exist;
\code{"reflect"} mirrors the matrix across the boundary; \code{"nearest"} repeats
the closest edge cell; \code{"constant"} pads with \code{edge_value}.}

\item{edge_value}{Single number used to pad when \code{edge = "constant"}.
\code{NA} is allowed and behaves as missing data under \code{na_policy = "omit"}.}

\item{na_policy}{How to treat missing values inside a window. The default
\code{"omit"} excludes them from the statistics (like \code{na.rm = TRUE}); a
window with no valid cells yields \code{NA}. \code{"propagate"} is the fast path:
no per-cell NA handling is compiled into the inner loop, and any \code{NA}
in a window makes the result \code{NA}. Use it when the input has no missing
values (or when spreading \code{NA} is acceptable) for maximum speed.}
}
\value{
An object of the same class and dimensions as \code{x} (dimnames are
preserved), containing the filtered values as doubles.
}
\description{
Smooths with weights that are the product of a spatial Gaussian (distance
from the centre cell, \code{sigma_d}) and a range Gaussian (difference from the
centre value, \code{sigma_r}), so averaging happens within regions of similar
value while sharp transitions survive.
}
\details{
Cells whose centre value is \code{NA} stay \code{NA} (the range weight needs the
centre). Cost grows with the window area; this is the slowest smoother in
the package.
}
\examples{
set.seed(1)
m <- matrix(rep(c(0, 10), each = 50), 10) + rnorm(100)
rf_bilateral(m, sigma_d = 1.5, sigma_r = 3)
}
\seealso{
\code{\link[=rf_guided]{rf_guided()}}, \code{\link[=rf_gaussian]{rf_gaussian()}}, \code{\link[=rf_median]{rf_median()}},
\code{\link[=rf_set_threads]{rf_set_threads()}}
}
